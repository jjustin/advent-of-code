# This is using ILP

I've used github.com/draffensperger/golp to solve part 2 for this day.
Look at the docs on how to set the dependency up correctly.
Generally you want to get the `lpsolve`'s `.so` file and it's header files and put them in the same folder as `golp` is installed/downloaded in.
